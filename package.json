{
  "name": "bun-monorepo-template",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run backend:dev\" \"bun run frontend:dev\"",
    "backend:dev": "bun --filter=backend run dev",
    "frontend:dev": "bun --filter=frontend run dev",
    "install:all": "bun install"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^8.2.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
